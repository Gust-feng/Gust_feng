#  Windows 常用命令

---

## 一、命令行环境

| 环境                  | 特点                      | 打开方式                                |
| --------------------- | ------------------------- | --------------------------------------- |
| **CMD（命令提示符）** | 经典命令行，兼容 DOS 命令 | `Win + R` → 输入 `cmd`                  |
| **PowerShell**        | 现代命令行，功能更强      | `Win + X` → 选择 PowerShell 或 Terminal |

> 💡 建议优先学习 PowerShell，它兼容 CMD 命令，还能处理对象、脚本。

---

## 二、基础命令

| 命令    | 功能                         | 示例                   |
| ------- | ---------------------------- | ---------------------- |
| `dir`   | 列出当前目录下的文件与文件夹 | `dir /a`               |
| `cd`    | 切换目录                     | `cd C:\Users`          |
| `cls`   | 清屏                         | `cls`                  |
| `exit`  | 退出命令行                   | `exit`                 |
| `echo`  | 输出文本或变量               | `echo Hello World`     |
| `type`  | 查看文本文件内容             | `type readme.txt`      |
| `copy`  | 复制文件                     | `copy a.txt D:\backup` |
| `move`  | 移动或重命名文件             | `move a.txt b.txt`     |
| `del`   | 删除文件                     | `del a.txt`            |
| `mkdir` | 创建文件夹                   | `mkdir test`           |
| `rmdir` | 删除文件夹                   | `rmdir test /s /q`     |

---

## 三、网络命令

| 命令          | 功能                 | 示例                       |
| ------------- | -------------------- | -------------------------- |
| `ipconfig`    | 查看网络配置         | `ipconfig /all`            |
| `ping`        | 测试网络连通性       | `ping 8.8.8.8`             |
| `tracert`     | 跟踪网络路径         | `tracert www.baidu.com`    |
| `netstat`     | 查看网络连接和端口   | `netstat -ano`             |
| `route print` | 查看路由表           | `route print`              |
| `arp -a`      | 查看 ARP 缓存        | `arp -a`                   |
| `nslookup`    | 查询 DNS 解析        | `nslookup google.com`      |
| `tasklist`    | 查看运行中的进程     | `tasklist`                 |
| `taskkill`    | 结束指定进程         | `taskkill /PID 1234 /F`    |
| `netsh`       | 网络配置与防火墙命令 | `netsh wlan show profiles` |

---

## 四、系统管理命令

| 命令           | 功能               | 示例                      |
| -------------- | ------------------ | ------------------------- |
| `systeminfo`   | 查看系统信息       | `systeminfo`              |
| `driverquery`  | 查看驱动信息       | `driverquery`             |
| `wmic`         | 查看硬件信息       | `wmic cpu get name`       |
| `sfc /scannow` | 检查系统文件完整性 | 以管理员身份运行          |
| `shutdown`     | 关机/重启命令      | `shutdown /r /t 0`        |
| `chkdsk`       | 检查磁盘错误       | `chkdsk C:`               |
| `powercfg`     | 电源管理命令       | `powercfg /batteryreport` |

---

## 五、文件与查找命令

| 命令      | 功能               | 示例                             |
| --------- | ------------------ | -------------------------------- |
| `find`    | 搜索文本内容       | `find "error" log.txt`           |
| `findstr` | 高级文本搜索       | `findstr /s /i "password" *.txt` |
| `tree`    | 树状显示文件结构   | `tree C:\Users /F`               |
| `attrib`  | 修改文件属性       | `attrib +h secret.txt`           |
| `where`   | 查找可执行文件路径 | `where python`                   |

---

## 六、PowerShell 常用命令

| 命令                     | 功能             | 示例                                                   |
| ------------------------ | ---------------- | ------------------------------------------------------ |
| `Get-Process`            | 查看进程         | `Get-Process | sort CPU -desc`                         |
| `Stop-Process`           | 结束进程         | `Stop-Process -Name chrome`                            |
| `Get-Service`            | 查看服务         | `Get-Service | Where-Object {$_.Status -eq "Running"}` |
| `Set-ExecutionPolicy`    | 设置脚本执行权限 | `Set-ExecutionPolicy RemoteSigned`                     |
| `Get-Command`            | 查找命令         | `Get-Command *net*`                                    |
| `Get-Help`               | 查看帮助文档     | `Get-Help Get-Process -Full`                           |
| `Get-NetIPConfiguration` | 查看网络配置     | `Get-NetIPConfiguration`                               |
| `Get-NetRoute`           | 查看路由表       | `Get-NetRoute`                                         |

---

## 七、练习建议

| 阶段       | 目标       | 建议练习                                     |
| ---------- | ---------- | -------------------------------------------- |
| **阶段 1** | 熟悉操作   | 用 `cd`、`dir`、`copy` 操作文件              |
| **阶段 2** | 网络理解   | `ipconfig`、`ping`、`tracert`、`route print` |
| **阶段 3** | 系统监控   | `tasklist`、`netstat`、`systeminfo`          |
| **阶段 4** | PowerShell | 用 `Get-*` 命令代替 CMD 命令                 |
| **阶段 5** | 脚本入门   | 写 `.bat` 或 `.ps1` 自动化脚本               |

---